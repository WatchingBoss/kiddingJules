1. **Abstract Data Dependency and Validation**
   a. Step 1. `main.py`:`check_source_parquet` (Lines 15-28) | We have: A hardcoded dictionary of tickers (`GAZP`, `SBER`) and exact row counts embedded directly in the main execution logic. The system scans and filters the entire dataset to validate length. -> Solution: Extract this hardcoded configuration into an external JSON or YAML file (`config.yaml`). Create a new `DatasetValidator` or `DataLoader` class in `utility.py` (or a dedicated `data.py`) that reads the config, uses Polars lazy evaluation (`.select(pl.len())`) to validate row counts quickly, and yields the `LazyFrame` cleanly to the engine without polluting `main.py`.
   b. Step 2. `main.py`:`main` (Lines 32-35) | We have: Hardcoded ticker initialization overriding configuration. -> Solution: Remove the explicit dictionary declaration. Instantiate the new `DataLoader` class, passing the path to the configuration file, and retrieve the validated data source automatically.

2. **Parameterize Indicator Generation (Eliminate Hardcoded Suffixes)**
   a. Step 1. `utility.py`:`add_emas` (Lines 29-35) | We have: EMA spans (10, 20) hardcoded in string interpolations like `f"ema10_{timeframe_label}"` within the `with_columns` expression. -> Solution: Modify the function signature to accept `fast_span: int` and `slow_span: int`. Dynamically build the column aliases using these inputs (`f"ema{fast_span}_{timeframe_label}"`) and return both the updated `LazyFrame` and a dictionary mapping logical names (e.g., `fast`, `slow`) to the physical column names.
   b. Step 2. `strategies/universal_ema.py`:`generate_signals` (Lines 26-52) | We have: Hardcoded references to `ema10` and `ema20` strings scattered throughout the base and higher timeframe joining logic. -> Solution: Update the `__init__` method of `UniversalEmaStrategy` to accept `fast_span` and `slow_span`. In `generate_signals`, call the updated `add_emas` method passing these parameters. Retrieve the dynamic column names from the returned dictionary and use them to construct the `buy_expr` and `sell_expr` filters programmatically.
   c. Step 3. `strategies/triple_ema_cross.py`:`generate_signals` (Lines 11-66) | We have: Hardcoded strings and identical inflexibility for the Triple EMA. -> Solution: Apply the same refactoring as `UniversalEmaStrategy`, decoupling the specific spans from the raw logic.

3. **Decouple the Strategy Registry and Dependencies**
   a. Step 1. `strategies/__init__.py`: (Lines 8-14) | We have: Obfuscated dependency resolution using `pkgutil.iter_modules` and `importlib.import_module` which hides explicitly loaded strategies and breaks static analysis. -> Solution: Delete the dynamic loading loops. Replace them with explicit `from .universal_ema import UniversalEmaStrategy` and `from .triple_ema_cross import TripleEMAStrategy` imports. The `@register` decorators will automatically trigger and remain functional without the "magic" code.

4. **Enhance Configuration Boundaries in Engine Execution**
   a. Step 1. `engine.py`:`BacktestEngine.run` (Line 7) | We have: Loose primitive arguments (`tp_pct: float = None, sl_pct: float = None`) explicitly bound in the signature. -> Solution: Introduce an `ExecutionConfig` dataclass (in `engine.py` or `config.py`) containing fields for `tp_pct`, `sl_pct`, `fast_span`, `slow_span`, and `exec_tf`. Change the `run` method signature to accept this single `ExecutionConfig` object.
   b. Step 2. `engine.py`:`BacktestEngine.run` (Lines 30-41) | We have: Direct references to `tp_pct` and `sl_pct` variables. -> Solution: Update the evaluation logic to safely reference `config.tp_pct` and `config.sl_pct`.

5. **Build the Parameter Permutation Generator**
   a. Step 1. `utility.py`:`generate_ema_grid` (Lines 38-60) | We have: A rudimentary grid generator that iterates over execution timeframes and hardcoded trend states, generating strings. -> Solution: Refactor or replace this with a robust combinatorial factory using `itertools.product`. It must yield an iterable sequence of fully instantiated `ExecutionConfig` objects representing the Cartesian product of defined EMA span pairs (e.g., [10/20, 9/21, 50/200]), TP/SL arrays (e.g., [0.5/0.2, 1.0/0.5]), and timeframes.

6. **Abstract Backtest Results and Final Reporting**
   a. Step 1. `engine.py`:`BacktestEngine.run` (Lines 80-100) | We have: A raw Polars DataFrame returned directly to `main.py` containing individual trades. -> Solution: Define a `BacktestResult` Domain Object (DTO) that encapsulates the execution configuration, the strategy name, and the final trade ledger. Return this `BacktestResult` object from the `run` method instead of the raw DataFrame.
   b. Step 2. `engine.py`:`report_metrics` (Lines 114-129) | We have: A static method printing the metrics of a single strategy straight to standard output. -> Solution: Convert this to a method on the new `BacktestResult` DTO (e.g., `result.get_metrics()`) that returns a single-row DataFrame summarizing the strategy's performance (Total Revenue, Pos/Neg Ratio, etc.).
   c. Step 3. `main.py`:`main` (Lines 44-54) | We have: A loop that tests four hardcoded strategies and prints metrics individually. -> Solution: Overhaul the loop. Iterate through the combinatorial grid generated in Milestone 5. Execute `engine.run(strategy, config)`. Append the returned `BacktestResult.get_metrics()` DataFrame to a master list.
   d. Step 4. `main.py`:`main` (End of file) | We have: No aggregation. -> Solution: Use `pl.concat` to merge the list of metric DataFrames into a global leaderboard. Execute a `.sort("Total_Revenue", descending=True)` operation and print the final sorted permutation ranking to achieve the Ultimate Goal.